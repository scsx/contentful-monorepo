// Aqui os .ts vão ser passados para .json individuais.

// After editing the .ts files (single source of truth), run the script to generate the corresponding .json files to \packages\cms-schema\src\schema\models.

// Deleted .ts files will be deleted in the target folder 

// É aqui que tem que se abortar o script quando há referencias entre models.

// Também tem que ler todos os .ts e todos os .json para perceber quais têm que ser criados/apagados. 